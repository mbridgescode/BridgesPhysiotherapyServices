# Runtime configuration
NODE_ENV=production
HOST=0.0.0.0
PORT=3000

# Database + encryption
MONGODB_URI=replace-with-your-mongodb-uri
ACCESS_TOKEN_SECRET=replace-with-strong-access-secret
REFRESH_TOKEN_SECRET=replace-with-strong-refresh-secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
DATA_ENCRYPTION_KEY=generate-a-32-byte-secret

# Network + security
CORS_ORIGIN=https://your-vercel-app.vercel.app
ENFORCE_HTTPS=true

# Email provider (Resend)
RESEND_API_KEY=
EMAIL_FROM_ADDRESS=no-reply@bridgesphysio.com

# Heidi AI Scribe (optional; keep the API key server-side)
# Request a Heidi integration/developer key, then set the region for that key.
HEIDI_API_KEY=
HEIDI_REGION=UK
# The public API documentation uses this base URL. Override it if Heidi gives
# you a region-specific production endpoint, e.g. https://uk.api.heidihealth.com/api/v2/ml-scribe/open-api
HEIDI_API_BASE_URL=https://registrar.api.heidihealth.com/api/v2/ml-scribe/open-api
HEIDI_WIDGET_URL=https://widget.heidihealth.com/widget/heidi.js
HEIDI_PRODUCT_NAME=Bridges Physiotherapy Services

# Links for password reset emails
FRONTEND_BASE_URL=https://your-vercel-app.vercel.app/reset-password

# File output directories (override if using external storage)
STORAGE_ROOT=./storage
INVOICE_STORAGE_PATH=./storage/invoices
RECEIPT_STORAGE_PATH=./storage/receipts
PDF_TEMP_PATH=./storage/tmp

# Chromium overrides for Puppeteer (optional)
CHROMIUM_REMOTE_EXEC_PATH=
CHROMIUM_LOCAL_EXEC_PATH=
CHROMIUM_PACK_URL=
